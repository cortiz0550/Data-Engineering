import requests
import os
import utils
import pandas as pd


def load_surveys(config):
    pass


""" Here we want to start with gathering the config files """
base_path = os.getcwd()
config_path = base_path + "\\config\\quickbase_config.json"
input_path = base_path + "\\data\\processed\\"


